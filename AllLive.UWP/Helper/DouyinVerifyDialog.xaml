<ContentDialog
    x:Class="AllLive.UWP.Helper.DouyinVerifyDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Title="抖音验证"
    PrimaryButtonText="完成验证"
    SecondaryButtonText="取消"
    DefaultButton="Primary"
    FullSizeDesired="True"
    MaxWidth="2000"
    MaxHeight="1200">

    <Grid Width="1400" Height="900">
        <muxc:WebView2 x:Name="webView2" />
        <ProgressRing x:Name="loadingRing" IsActive="True" 
                      HorizontalAlignment="Center" VerticalAlignment="Center"
                      Width="50" Height="50"/>
        <TextBlock x:Name="tipText" Text="请在页面中完成验证，验证完成后点击「完成验证」" 
                   VerticalAlignment="Bottom" HorizontalAlignment="Center"
                   Margin="0,0,0,10" Foreground="Gray"/>
    </Grid>
</ContentDialog>
